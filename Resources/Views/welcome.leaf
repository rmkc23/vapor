#extend("base")

#export("head") {
	<title>My App</title>

    <link href="https://fonts.googleapis.com/css?family=Quicksand:400,700,300" rel="stylesheet">
	<link rel="stylesheet" href="/styles/app.css">
}

#export("body") {
    <div class="message">
        <div class="#(icon)"></div>
        <h1>Hi! It's #(time)</h1>
        <h2>Currently: #(temp)&deg;, #(current-summary). #(hourly-summary)</h2>
    </div>
}
